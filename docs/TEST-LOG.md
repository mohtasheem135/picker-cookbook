# TEST-LOG — verifications of availability-datetime-picker v0.1.0

Consumer-side test log. Every check of the package performed through or
alongside this cookbook gets a row. Newest last.

| Date | Scenario | Method | Result |
| ---- | -------- | ------ | ------ |
| 2026-07-15 | Tarball installs as a real npm consumer | `npm install` with `file:../availability-datetime-picker-0.1.0.tgz` (only `dist/` present, real exports map) | ✅ installed clean, peer deps react/react-dom 19 satisfied |
| 2026-07-15 | Types resolve from the packed artifact | `tsc --noEmit` across 17 pages + 14 demos importing root and `/core` | ✅ zero errors — all prop/hook/type names checked against shipped `.d.ts` |
| 2026-07-15 | SSR/RSC production build | `next build` (Next 16, Turbopack): 20 static routes + 1 dynamic API route | ✅ compiled; all docs pages prerendered; no hydration-unsafe imports flagged |
| 2026-07-15 | `/core` subpath is server-safe | `app/api/quote/route.ts` imports `createAvailabilityEngine` from `availability-datetime-picker/core` in a Route Handler | ✅ builds and executes server-side (no React, no 'use client' leakage) |
| 2026-07-15 | Engine test matrix, zone-independent | package repo: `npm run test:tz` (vitest under `TZ=UTC` then `TZ=Pacific/Kiritimati`) | ✅ 50/50 tests pass in both zones |
| 2026-07-15 | Package static gates | package repo: `npm run typecheck`, `npm run lint`, `npm run build` (fix-extensions + use-client directive guard + CSS-collision guard: 161 rules) | ✅ all clean; rebuild produced zero diff in committed `dist/` (reproducible) |
| 2026-07-15 | Publish readiness of the artifact | `npx publint` (packs and lints) · `npx attw --pack .` | ✅ publint "All good". attw: root + `/core` green for ESM/bundler; CJS shows expected "dynamic import only" (ESM-only by design); `/styles.css` "resolution failed" is attw checking TS resolution on a CSS export — non-issue, bundler resolution proven by this app |
| 2026-07-15 | All routes serve under production SSR | `next start` + HTTP GET on all 18 routes (home + 17 docs pages) | ✅ 18× HTTP 200; SSR HTML contains expected content and `bdp`-classed picker triggers |
| 2026-07-15 | Server rejects what the UI would never allow | POST `/api/quote`: valid range and a range overlapping the seeded booked trip | ✅ valid → 200 `{ok:true}`; tampered → 422 `OVERLAPS_BLOCKS` with offending block + human-readable message |
| 2026-07-15 | Interactive browser click-through (popover/drawer, blocked-day reasons, retheme, breakpoints) | manual — `npm run dev` | ⏳ pending human pass; all machine-checkable layers green |
| 2026-07-15 | Workspace restructure: package moved to sibling `../availability-datetime-picker/`, tarball re-packed (79 files), dependency path updated | `rm -rf node_modules package-lock.json && npm install` + `next build` | ✅ install clean, all 21 routes build; consumer unaffected by relocation |
| 2026-07-15 | Package v0.1.0 feature update: `valueFormat` wire formats + `showFooter` + footer border fix (`bdp-bw-hairline-t`) | package gates: typecheck, lint, `test:tz` (59 tests ×2 zones incl. 9 new codec tests), build guards; tarball re-packed and re-installed here | ✅ all green; new utility present in shipped styles.css |
| 2026-07-15 | New codec exports usable from the packed artifact | `node` ESM import of `/core`: `encodeInstant(…, 'iso')`, `decodeDay(epoch-s, 'Asia/Kolkata')`, `encodeDay(…, 'epoch-seconds')` | ✅ correct zone-sensitive results at runtime |
| 2026-07-15 | valueFormat via the components (typed) | `value-formats.tsx` demo — SingleDateTimePicker `valueFormat='iso'` (string state) + SingleDatePicker `valueFormat='epoch-seconds'` (number state) compile against shipped `.d.ts` and render | ✅ typecheck + `next build` green |
| 2026-07-15 | Cookbook UI: dark/light toggle + code-block readability fix | inline-code CSS no longer leaks into shiki blocks (`:not(pre) > code`); `data-theme` boot script + `--bdp-*` dark tokens; `next build` | ✅ builds; manual visual pass recommended |
| 2026-07-15 | BUG (user-reported): DateTimeRangePicker hid the time-slot list right after picking a pickup day | Root cause: `selectPickupDay` auto-advances `editing` to 'return' while return is null, and the slot panel only rendered for the edited field. Fixed in package `src/ui/booking-date-time-picker.tsx` — panel falls back to the endpoint that has a value (`slotsField`) | ✅ fix present in installed dist; typecheck/lint/build/pack green; cookbook rebuilt — needs a manual click-through to confirm |
| 2026-07-15 | BUG (user-reported): partially booked days rendered with no marker | Root cause found by `grep amber dist/styles.css` = 0 hits — the dot class used nonexistent `bg-amber`, silently dropped by Tailwind. Fixed with new `--bdp-warning` token + `after:bg-bdp-warning` | ✅ `bdp-warning` present ×4 in shipped CSS; 163 rules pass collision guard |
| 2026-07-15 | User question: "Trip too short" became "Unavailable" on the same slots | Engine reproduction (block ending 4:00 PM, 24 h min): pickup list marks in-block times `blocked` ("Unavailable") and the exact block-end slot ENABLED (half-open ✓); return list marks pre-minimum times `min-duration` ("Trip too short"). Not a bug — different rules on different lists; visible now because the pickup list auto-shows since the slot-panel fix | ✅ engine verified correct; explained on the day-states page |
| 2026-07-15 | User question: scrollbar over the popover on hover | The popover clamps to Radix's available viewport height and scrolls only when the two-month layout doesn't fit — necessary fallback, but the stock scrollbar was fat/unthemed. Added `bdp-scroll` utility (thin, token-colored) to the popover scroller and slot list | ✅ shipped in styles.css |
| 2026-07-15 | New slot-hint options | `showSlotHints` (default true) + `slotHintLabels` on both date+time components; defaults exported as `SLOT_REASON_LABELS` | ✅ typecheck against shipped d.ts; gates green |
| 2026-07-15 | Missing-timeZone behavior (for docs) | `createAvailabilityEngine([], {})` in Node: no throw; `todayKey` derived in the RUNTIME zone (Asia/Calcutta on this machine) | ✅ documented on the timezones page: silent runtime-zone fallback → SSR mismatch risk |
| 2026-07-15 | Performance & bundle measurements | `wc -c`/`gzip -9` over installed dist; `performance.now()` engine loops; gzip over `.next/static/chunks` | ✅ JS 96.8 KB/21.6 KB gz; /core 29.6/7.7; CSS 13.7/3.3; engine build 0.016 ms (100 blocks); dayInfo 0.002 ms; pickupSlots 0.08 ms; validateRange 0.0006 ms; whole 18-page app 318 KB gz |
| 2026-07-15 | Final end-to-end: consume the PUBLISHED registry package | availability-datetime-picker@0.1.0 published to npm; dependency switched from local tarball to `^0.1.0` (Vercel build had failed on the missing sibling tarball); fresh install resolves from registry.npmjs.org; `next build` | ✅ 22/22 pages build against the public artifact — the plan's final verification gate |
| 2026-07-15 | Local/published package toggle | `npm run use:local` (pack + `npm install --no-save file:<tgz>`) then `npm run use:npm` (`npm ci`) | ✅ round trip clean. Verified by sentinel string: registry 0.1.0 renders "the car's local time", local build renders "the resource's local time" — `.next` output matched the expected package in both directions |
| 2026-07-15 | Local mode cannot reach a deploy | Diffed `package.json` + `package-lock.json` before/after `use:local` | ✅ both byte-identical; `--no-save` records the `file:` resolution only in `node_modules/.package-lock.json`, which is gitignored |
| 2026-07-15 | `turbopack.resolveAlias` rejected as a switching mechanism | Aliased the package to an absolute path, then to a nonexistent module, under `next build` | ❌ unusable: absolute paths error ("server relative imports are not implemented"), and a missing target **silently falls back** to node_modules — build reported success while bundling the registry copy. Tarball `--no-save` used instead |
| 2026-07-15 | Pre-push guard rejects a hand-edited `file:` dep | Set `dependencies.availability-datetime-picker` to a `file:` tarball, ran `npm run check:registry-dep` | ✅ exit 1 with remediation; passes again once restored to `^0.1.0` |
| 2026-07-15 | Build + typecheck against the local build | `use:local` → `next build` + `tsc --noEmit` | ✅ compiled successfully, 22 routes; typecheck clean against the local `.d.ts` (no separate tsconfig needed — node_modules holds the local package) |
| 2026-07-19 | BUG (user-reported): dark-mode body text unreadable | Root cause: `.doc p, .doc li` were pinned to a hardcoded `#3d3835` (never switched with the theme), so body copy sat near-black on the `#131211` dark background; the props-table `REQUIRED` tag had the same theme-independent `#b45309`. Fixed by routing both through new theme-aware `--prose`/`--required` tokens (light unchanged; dark `#d9d3cc`/`#f0a860`) | ✅ `next build` clean, 22/22 routes prerender; hardcoded text colors that remain sit only on always-dark code blocks / brand buttons |
| 2026-07-19 | New feature: interactive Playground page | Added `/docs/playground` — component switcher + live prop controls (timeZone, valueFormat, precision, label, blocks, showFooter, disabled) driving the real `SingleDatePicker`/`SingleDateTimePicker`/`DateOnlyRangePicker`/`DateTimeRangePicker`, a live-generated code snippet, and a "What onChange returned" panel (raw value + typeof + decoded) that answers the format question. `npm run typecheck` + `npm run build` | ✅ typecheck clean; 21/21 routes build incl. /docs/playground; confirmed `SinglePrecision` is `'slots' \| 'minute'` (singular) against shipped d.ts |
| 2026-07-19 | BREAKING rename `BookingDateTimePicker` → `DateTimeRangePicker` | Package: renamed export + `BookingDateTimePickerProps`→`DateTimeRangePickerProps`, `BookingValueOf`→`DateTimeRangeValueOf`, file `src/ui/booking-date-time-picker.tsx`→`date-time-range-picker.tsx`, added a `major` changeset; headless `useBookingPicker`/`BookingValue` unchanged. Cookbook: updated all usages, renamed route `/docs/booking-date-time-picker`→`/docs/date-time-range-picker` + doc mirror + nav + INDEX. `npm run use:local` → package typecheck + cookbook typecheck + build | ✅ pkg tsc clean; cookbook 21/21 routes build incl. renamed route; ⚠️ LOCAL-ONLY — registry 0.1.0 has no `DateTimeRangePicker`, so this is NOT deploy-safe until the package is published (next major) and the cookbook dep bumped |
| 2026-07-19 | Playground: timeZone "none → browser zone" option | Added a `none` timeZone option that omits the prop; picker falls back to the runtime zone (relies on the optional-`timeZone` feature). Return panel shows the resolved zone; generated snippet drops the `timeZone` line. Verified fallback path against the local build | ✅ typecheck + build clean; ⚠️ depends on unpublished optional-`timeZone` — LOCAL-ONLY, not deploy-safe against registry 0.1.0 until published + dep bumped |
| 2026-07-19 | Playground: engine `config` knobs | Added controls for `minRentalMinutes`/`leadTimeMinutes`/`slotIntervalMinutes`/`maxAdvanceDays`, passed via the `config` prop on the date+time components only (date-only pickers show a note — no config prop). Per-component defaults surfaced (DateTimeRangePicker 420/60/30/365 vs SingleDateTimePicker 0/0/30/365 via singleInstantConfig); knobs reset to baseline on component switch; generated snippet emits `config={{…}}` with only the changed knobs | ✅ typecheck + build clean, 21/21 routes; ⚠️ still LOCAL-ONLY (rename + optional-timeZone unpublished) |
| 2026-07-19 | Playground: ⓘ help tooltips on config knobs | Added a reusable `InfoTip` (components/site/info-tip.tsx) + CSS; each of the four EngineConfig knobs (minRentalMinutes/leadTimeMinutes/slotIntervalMinutes/maxAdvanceDays) now has a hover/focus tooltip explaining its meaning and engine default | ✅ typecheck + build clean, 21/21 routes |
| 2026-07-19 | Playground: added `turnaroundMinutes` config knob | Fifth EngineConfig knob (with ⓘ tooltip) alongside the existing four; baseline 0 for both date+time components; flows through configOverrides + generated `config={{…}}` automatically. Effect is visible when demo blocks are on (pads blocks both sides) | ✅ typecheck + build clean, 21/21 routes |
| 2026-07-19 | Agent-facing docs: /llms.txt, /llms-full.txt, /docs/for-agents | Added `lib/docs.ts` (reads docs/*.md mirrors, builds both files in nav order), two force-static route handlers, and a For-agents page that renders `docs/for-agents.md` via shiki. Nav + INDEX updated. Links honour NEXT_PUBLIC_SITE_URL (root-relative fallback) | ✅ typecheck + build clean; 3 routes prerender static; llms.txt = valid llmstxt.org index, llms-full.txt = 1232 lines (for-agents guide first, then all pages); ⚠️ public URL still gated on publishing the package (registry 0.1.0 lacks DateTimeRangePicker) before the Vercel deploy |
